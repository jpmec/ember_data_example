<form {{action save on="submit"}} class="form-horizontal">
  <legend>Edit Todo</legend>

  {{partial "todo_edit_fields"}}

  <div class="form-actions">
    {{submitButton "Update"}}
    <button class="btn" {{action cancel}}>Cancel</button>
  </div>
</form>
